# =============================================================================
# AgriProfit API - Production Environment Configuration
# =============================================================================
# CRITICAL: All CHANGEME values MUST be replaced with secure values!
# This file serves as a template - actual production values should be
# set via environment variables or a secure secrets manager.

# =============================================================================
# ENVIRONMENT
# =============================================================================
ENVIRONMENT=production
DEBUG=false

# =============================================================================
# DATABASE
# =============================================================================
# Use secure credentials from secrets manager
DATABASE_URL=postgresql+psycopg://agriprofit:CHANGEME@prod-db.agriprofit.in:5432/agriprofit
DATABASE_POOL_SIZE=20
DATABASE_MAX_OVERFLOW=40
DATABASE_ECHO=false

# =============================================================================
# JWT AUTHENTICATION
# =============================================================================
# CRITICAL: Generate a secure random key: openssl rand -hex 32
JWT_SECRET_KEY=CHANGEME-generate-with-openssl-rand-hex-32
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# =============================================================================
# OTP CONFIGURATION
# =============================================================================
OTP_LENGTH=6
OTP_EXPIRE_MINUTES=5
OTP_COOLDOWN_SECONDS=60
OTP_MAX_ATTEMPTS=3

# =============================================================================
# SMS PROVIDER
# =============================================================================
SMS_PROVIDER=fast2sms
FAST2SMS_API_KEY=CHANGEME

# Alternative: Twilio
# SMS_PROVIDER=twilio
# TWILIO_ACCOUNT_SID=CHANGEME
# TWILIO_AUTH_TOKEN=CHANGEME
# TWILIO_PHONE_NUMBER=+91XXXXXXXXXX

# =============================================================================
# CORS
# =============================================================================
# Restrict to production domains only
CORS_ORIGINS=https://agriprofit.in,https://www.agriprofit.in,https://app.agriprofit.in
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_ALLOW_HEADERS=Content-Type,Authorization,X-Request-ID

# =============================================================================
# RATE LIMITING
# =============================================================================
# Redis cluster for high availability
REDIS_URL=redis://prod-redis.agriprofit.in:6379/0
RATE_LIMIT_CRITICAL=5/minute
RATE_LIMIT_WRITE=30/minute
RATE_LIMIT_READ=100/minute
RATE_LIMIT_ANALYTICS=50/minute

# =============================================================================
# LOGGING
# =============================================================================
LOG_DIR=/var/log/agriprofit
LOG_LEVEL=WARNING
LOG_RETENTION_DAYS=90
LOG_JSON_FORMAT=true

# =============================================================================
# SECURITY
# =============================================================================
ALLOWED_HOSTS=agriprofit.in,www.agriprofit.in,api.agriprofit.in
HTTPS_REDIRECT=true

# =============================================================================
# API METADATA
# =============================================================================
API_TITLE=AgriProfit API
API_VERSION=1.0.0
API_CONTACT_EMAIL=support@agriprofit.in

# =============================================================================
# MONITORING
# =============================================================================
# Sentry for error tracking
SENTRY_DSN=CHANGEME
